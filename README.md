# Домашние задания по курсу "Базовый Python"

Этот репозиторий содержит домашние задания по курсу Python с автоматическими тестами.

## 📋 Содержание

- [Установка и настройка](#установка-и-настройка)
- [Запуск тестов](#запуск-тестов)
- [Работа с Docker](#работа-с-docker)

## 🛠️ Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd docker
```

### 2. Создание виртуального окружения


```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
# macOS/Linux:
source venv/bin/activate

# Windows:
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
# Установка зависимостей для разработки (включает pytest и другие инструменты)
pip install -r requirements-dev.txt

# Если нужны только основные зависимости
pip install -r requirements.txt
```

## 🧪 Запуск тестов

### Запуск всех тестов

```bash
pytest
```

### Запуск тестов конкретного домашнего задания

```bash
# Домашнее задание 3 (требует Docker)
pytest testing/test_homework_03/
```

### Запуск тестов с подробным выводом

```bash
pytest -v
```

### Запуск тестов с выводом print()

```bash
pytest -s
```

## 🐳 Работа с Docker

### Для homework_03

#### Сборка Docker образа

```bash
cd homework_03
docker build -t fastapi-app .
```

#### Запуск контейнера

```bash
# Запуск в фоновом режиме с пробросом порта
docker run -d -p 8000:8000 fastapi-app

# Запуск с выводом логов
docker run -p 8000:8000 fastapi-app
```

#### Проверка работы приложения

```bash
# С помощью curl
curl http://localhost:8000/ping/

# Или откройте в браузере
# http://localhost:8000/ping/
```

Ожидаемый ответ:
```json
{"message": "pong"}
```

## 🔧 Технологии

- **Python 3.9+**
- **FastAPI** - современный веб-фреймворк
- **uvicorn** - ASGI сервер
- **pytest** - фреймворк для тестирования
- **Docker** - контейнеризация приложений

